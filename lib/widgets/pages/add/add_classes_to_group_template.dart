import 'package:flutter/material.dart';

class AddClassesToGroup extends StatelessWidget {
  const AddClassesToGroup({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}

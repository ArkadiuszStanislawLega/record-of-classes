import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

class AddParentsTemplate extends StatelessWidget {
  const AddParentsTemplate({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Column(
      children: const [Text('Create Parents Template')],
    );
  }
}
